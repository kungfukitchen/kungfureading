---
interface Props {
  cta: string;
  link: string;
}

const { cta, link } = Astro.props;
---

<a href={link}>
  <button class="cta shadow font-bold bg-red text-white px-4 py-2 rounded"
    >{cta}</button
  >
</a>
